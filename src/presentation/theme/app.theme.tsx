import {StyleSheet} from 'react-native'

export const colors = {}

export const styles = StyleSheet.create({
  globalMargin: {
    marginHorizontal: 20,
  },
  title: {
    fontSize: 20,
    marginBottom: 10,
  },
})
